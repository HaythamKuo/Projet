{
  "name": "dollproject",
  "private": true,
  "version": "1.0.0",
  "description": "Root package.json for project control",
  "scripts": {
    "start": "cd backend && npm run start",
    "server": "cd backend && nodemon index.js"
  }
}
