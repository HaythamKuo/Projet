// const path = "/prodcuts/123";

// const single = path.split("/").filter(Boolean);
// //console.log(single);

// const ans = single.map((s, index) => {
//   const to = "/" + single.slice(0, index + 1).join('/');
//   return to;
// });

// console.log(ans);
